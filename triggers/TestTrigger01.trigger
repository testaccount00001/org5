trigger TestTrigger01 on Testing_1__c (after insert)
{
    if(Trigger.isAfter && Trigger.isInsert)
    {
        //UserTriggerH   andler hand                                                ler = new UserTriggerHandler();
        //ha  ndler.createR            eposit  oryPermission(Trigger.Ne           w);
 
    }
}